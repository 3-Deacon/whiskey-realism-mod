# Whiskey Realism — Repository Memory

> Last updated: 2026-05-11. Format aligned with Codex project-instruction behavior documented as of April 2026.

This file is the project memory index for agents and maintainers. It is intentionally short and durable. Keep detailed history in `docs/handoff.md`, shipped patch facts in `docs/patch-catalog.md`, and implementation details in source/spec/plan files.

## How Agents Should Use This

- `AGENTS.md` is the canonical auto-loaded instruction file for Codex.
- `MEMORY.md` is not assumed to be auto-loaded by Codex; read it when resuming this repo, investigating prior decisions, or updating handoff state.
- `docs/handoff.md` is the authoritative long-form session-start handoff.
- `docs/patch-catalog.md` is the authoritative shipped-patch inventory.
- `docs/bug-fixes/` is the cross-cutting vanilla bug-fix workstream for narrow confirmed defects and runtime hazards that are not new doctrine slices.
- `docs/superpowers/specs/` holds active design specs; shipped specs live in `docs/superpowers/specs/archive/` (see archive `README.md` for the index). Same split for `docs/superpowers/plans/`.
- Source-of-truth order remains: shipped code > `docs/patch-catalog.md` > per-patch design doc > umbrella spec > archived plan.

## Current Checkpoint

- Current release is `v0.2.2`.
- **Regular campaign command movement anchor correction (2026-05-11):** vanilla regular campaign formations are still `Regiment` components; the C# class name does not mean the visible command is literally a battlefield regiment. UI labels use `overridesymbol` (`13` Brigade, `14` Division, `15` Corps, `16` Army), while `SetGroupFormation` only accepts command/group formations with `unittyp > 13` and returns immediately for `unittyp <= 13`. Effective echelon still depends on hierarchy depth, `GamePrefs.commandhierarchyshift`, `overridesymbol`, and attached-unit structure, so Whiskey command writers must use ledger/hierarchy membership as authority and use `unittyp > 13` only as a formation-API guard. Non-W&L campaign movement is `BattleUI.CheckPathSetting` / `CheckGroupRotation` -> `BattleUnits.SetWaypoint` -> `SetGroupFormation` / `GetHierarchyTree`. `AIBattle.CheckCurrentOrderUpdate` hard-gates on `DLC_WL.dlc_scenarioactive` and is W&L current-order/message machinery only; do not use it as the regular campaign movement API.
- **Full-spectrum tactical command doctrine implemented and hash-deployed (2026-05-11 living-doc checkpoint):** #61 `BattleCommandPostureExecutorPatch` plus the operations-ledger command system now drive doctrine consumers for #45 stance, #41 charge, B8 reserve/fallback, and B7 artillery. Release/default config is `Tactical Commander Mode = Active`; `MonitorOnly` runs vision/ledger/task assignment/monitoring telemetry without writes; `Off` is rollback. The system writes only through bounded vanilla `BattleUnits.ChangeStance`, `BattleUnits.SetWaypoint`, and `BattleUnits.SetGroupFormation` after `AIBattle.AdjustGroupFormations`, with W&L/player/rout/order-pending/recent-order/close-engagement gates. The 2026-05-10/11 Hampton/1st Brigade investigations are addressed in code: interrupted non-reserve hold/fallback tasks are illegal idle, command nodes resolve by GameObject id first with component-id fallback, severe local overmatch can assign `Fallback` before `Fix`/`Main`, visible formation mismatch can force urgent reform/facing, close defensive/fallback local reform can bypass stalled courier state, visible formed enemy contact raises objective confidence, high-odds visible weak points can attack/counterstroke even under defensive macro, skirmisher/screen-only contact does not expose the main line, doctrine charge allow cannot bypass W&L/player/orchestrator/B6c gates, and doctrine reserve deny restores movement state through #59 rollback helpers instead of leaving stale paths. Latest follow-up fixes: Active observer posture telemetry now uses the side ledger's real commander mode instead of hardcoded monitor-only simulation; committed high-odds attacks no longer soft-abort solely because reserves are low while the primary objective remains favorable; #61 posture writes use `TacticalOrderSettlementGate.HasBlockingPendingOrder`, so stalled `orderState=1` + `pathInterrupted=True` + stale/empty paths + no active move can recover instead of being blocked as `order-pending`; real queued courier orders still block; doctrine `FallBackToLine` overrides interrupted stale recovery with the doctrine fallback target instead of replaying the last safe waypoint; visible enemy-line fallback objectives now clear committed attack confidence; visible enemy-line objectives are appended alongside vanilla anchors and preferred over inert generic anchors when exposed with favorable odds; named formed detachments no longer zero sector enemy/confidence by name alone; and #61 suppresses duplicate same-target waypoint writes to reduce assembly-area churn. Current `main`/`dist/WhiskeyRealism.dll` and the deployed BepInEx plugin match SHA-256 `969f1aec126057ac8453cefbee212a099db2eaef277a3c2d35490a47b0125897` (933376 bytes); console harness is **841 PASS / 0 FAIL** and `./build.sh` is clean. Current local BepInEx config explicitly has `Tactical Commander Mode = Active` plus tactical orchestrator/logging flags enabled, including `Enable Tactical Orchestrator Charge Gate = true`. Do not claim smoke until a fresh battle log on the latest deployed DLL proves `[TacticalCommanderMode] mode=Active`, `[TacticalOpsLedger]`, `[TacticalCommandAssignment]`, `[TacticalCommandPosture]`, `[TacticalPostureSummary]`, `[TacticalGroupDecision]`, `[TacticalDoctrineCharge]`, `[TacticalOrchestratorChargeGate]`, bounded reserve/fallback rows, no repeated errors, and no player-subordinate retasking. Current behavior, config, smoke checklist, and rollback live in `docs/tactical-operations-ledger.md`; orchestrator status lives in `docs/tactical-orchestrator.md`.
- **Tactical terrain/facing discipline merged to main (2026-05-10):** #60 `TacticalDeploymentTerrainDisciplinePatch` plus #58 deployment terrain/facing telemetry are on `main`. The behavior does not replace vanilla pathfinding; it runs after `BattleUnits.DoPlacementAIUnitsWithinDeploymentzoneNew`, mirrors vanilla deployment eligibility, samples water/deployment-zone/footprint evidence plus explicit visible-enemy bearing, and corrects only clear AI terrain/deployment-zone failures through vanilla `SetGroupFormation`. Terrain-safe groups are not rotated for facing alone. After correction it mirrors vanilla cleanup by clearing only `lastsetwaypointposition` and `immediateunitplacement` on the corrected group tree. #60 was verified at SHA-256 `b00e03bd7e635e981380459e09a0d52a19d635c22c49bd340b403dacfbdf4cf8` (841216 bytes; 717 PASS) and is included in the current full-spectrum doctrine DLL `969f1aec126057ac8453cefbee212a099db2eaef277a3c2d35490a47b0125897` (933376 bytes; 841 PASS). The live local config has `Enable Tactical Deployment Terrain Discipline = true` for focused smoke. Runtime smoke remains pending; living runtime guidance is `docs/tactical-terrain-facing-discipline.md`.
- **Tactical orchestrator Slice 0 — dynamic command-node tree merged to main (2026-05-10):** Adds read-only generic command nodes from vanilla `Regiment` hierarchy instead of hard-coded corps/division/brigade orchestrator classes. New pure surface: `CommandNodeSnapshot`, `CommandTreeSnapshot`, `CommandNodeIntent`, `CommandTreeBuilder`, `CommandTreeIntentAllocator`, and `CommandIntentResolver`; runtime adapter: `CommandTreeRuntime` reads `BattleUnits.completeunitlist`, `Regiment.GetAttachedUnitsReg(... directonly: true ...)`, `Regiment.parentregiment`, and `GamePrefs.commandhierarchyshift`. `ArmyOrchestrator` stores current command tree/intents and resolves exact command-node intent, then falls back to O3 direct-child ids (`child-{id}` / `synth-army-{id}`) when the tree is unavailable or misses the group. Runtime registration is signature-based per tick, synthetic-only startup trees can be replaced by a real hierarchy, and real trees are not downgraded to transient synthetic fallback. No vanilla battle-state writes. Slice 0 smoke confirmed bounded `[TacticalCommandTree]` markers for both AI sides (side 0 `root=synth-root-0 nodes=11 maxDepth=2`; side 1 `root=node--26568 nodes=9 maxDepth=1`) with no `Exception`, `ERROR`, `missing-anchor`, or `failed` hits in the focused scan.
- **Tactical orchestrator Slice 1 — reserve commitment gate merged to main and hash-deployed locally (2026-05-10):** Adds pure `TacticalReserveCommitGate`, default-off #59 `BattleReserveCommitGatePatch` under `Enable Tactical Orchestrator Reserve Commit Gate`, and command-aware #57 reserve-list bias. #59 snapshots vanilla `AIBattle.CheckUseOfReserves` side effects, resolves command-node intent for the calling group, and rolls back newly-created direct reserve support paths only for command nodes resolved as `Reserve`; allowed movement remains vanilla-owned and still eligible for #56 delayed-order conversion. #57 now skips candidates whose command-node role resolves to `Reserve`, while unresolved command-tree state fails open to preserve existing behavior. Follow-up `f377ba2` hardens #59 snapshots after logs showed `no-vanilla-commit` despite #56 observing direct reserve paths. Slice 1 was individually verified at **689 PASS / 0 FAIL** and hash `82b1d91a...`, then merged in DLL `b00e03bd...` (717 PASS); current full-spectrum doctrine DLL is `969f1aec126057ac8453cefbee212a099db2eaef277a3c2d35490a47b0125897` (933376 bytes; 841 PASS). Focused gate-OFF/gate-ON smoke is still pending after a fresh game run.
- **Tactical orchestrator Slice 3 — #41 charge gate merged to main and hash-deployed locally (2026-05-10):** #41 `BattleChargeGatePatch` remains the owner for `AIBattle.MicroAICheckForCharges` and adds default-off `Enable Tactical Orchestrator Charge Gate`; the current live BepInEx config sets it true for Active smoke. When enabled, #41 consults command-node intent before vanilla `SetMovementMode(3)` charge initiation, after W&L/player protection and before the B6c explicit `DenyCharge` defense-in-depth check. Missing command-tree state fails open. The Screen exception is conservative and only treats the vanilla charge-target candidate `unitrange.enemyinrangereg[0]` as routed-target evidence; any other routed visible enemy is not enough. Deny logs use `[TacticalOrchestratorChargeGate] action=deny`. Full-spectrum doctrine now adds a main-line-exposed requirement before doctrine `Allow` can support a formed charge. Slice 3 was individually verified at hash `ac77b132...`, then merged in DLL `b00e03bd...` (717 PASS); current full-spectrum doctrine DLL is `969f1aec126057ac8453cefbee212a099db2eaef277a3c2d35490a47b0125897` (933376 bytes; 841 PASS). Living status is in `docs/tactical-orchestrator.md`; focused gate-OFF/gate-ON in-game smoke remains pending after a fresh battle run.
- **Tactical orchestrator O3 — ArmyIntent direct-child enrichment + #42 gate extension shipped on `orch/o2-intent` (2026-05-09):** Rescoped from a separate `CorpsOrchestrator` echelon to additive `ArmyOrchestrator.ArmyIntent.DirectChildIntents` + #42 W&L feud-action gate consultation after adversarial review found vanilla has no corps tier (`commandhierarchyshift` makes hierarchy depth era-dependent), and #42 already owns the gate surface (`AIBattle.CheckForFeudGroupActions`). The slice adds `DirectChildIntent`, `TacticalDirectChildAllocator` (Main / SupportMain / RefuseRight / RefuseLeft / Screen / Reserve / Fallback / Fix role assignment), per-side direct-child discovery walking `BattleUnits.completeunitlist` filtered by alliance, and `TacticalDirectChildGate.Decide` consultation inside #42 between the W&L decision and `bunits.SetWaypoint`. In the shipped final form, `DecideAxis` for Main/SupportMain compares `IntendedTargetSector` against the orchestrator's `AxisSector` OR the child's own `PrimarySector` (sector-based comparison after the integration-review `f2bd51a` fix; the interim relative-to-group bearing comparison from `ee7a501` was a tautology at the #42 call site and was superseded). Default-off behind `Enable Tactical Orchestrator Direct-Child Gate`; gate-OFF run is a pure no-op against vanilla feud movement. Console harness: **661 PASS / 0 FAIL** (612 baseline + 49 new O3 tests, including the 4 `ParseInstanceIdFromChildId` regression cases added in `f2bd51a`). `./build.sh` passed with 0 warnings / 0 errors. Local `dist/WhiskeyRealism.dll` and deployed BepInEx plugin match SHA-256 `2a9e1e2f3b4bb547254fe69d02c355b69f65f02016875cd4ca298c90a03237b0`. Final commit `c450ad4`. Six fixes shipped (five smoke-driven + one integration-review): `f19901e` ResolveTargetSector returns `-1` so primary-sector coercion engages; `09d32c4` gate falls back to `synth-army-{id}` when `child-{id}` lookup misses; `9161276` runtime derives real per-child primary sector + flank exposure from `completeunitlist`; `ee7a501` gate bearings switched to relative-to-group framing (interim — superseded for `DecideAxis` by `f2bd51a`, retained for `DecideFallback`); `a17ee9c` discovery walks `completeunitlist` filtered by alliance with a `StartsWith` parser preserving negative GameObject InstanceIDs; `f2bd51a` integration-review fix wires `AxisSector` into `DecideAxis` (sector-based comparison replacing the bearing tautology; `AxisSector` was a dead data path before this fix) and moves `ParseInstanceIdFromChildId` to the test-included partial as `internal static` with 4 regression tests covering positive/negative x child/synth-army parsing. Gate-OFF and gate-ON smoke verified — gate-OFF emitted `[TacticalDirectChildDiscovery]` with distinct parent-army IDs per side, distinct child sectors (4/6/10), and differentiated allocator roles (Main/RefuseRight/Fallback/Reserve), zero exceptions; gate-ON same positives plus zero deny lines because vanilla `CheckForFeudGroupActions` did not trigger `ai_feudstance >= 0` during the short battle (deny path harness-verified in 9 Task 8 unit tests plus the 4 parser cases). Rescoped O3 spec lives at [`docs/superpowers/specs/archive/2026-05-09-tactical-orchestrator-o3-corps-design.md`](docs/superpowers/specs/archive/2026-05-09-tactical-orchestrator-o3-corps-design.md). **Active next workstream:** [`docs/superpowers/specs/2026-05-09-tactical-orchestrator-remaining-patches-design.md`](docs/superpowers/specs/2026-05-09-tactical-orchestrator-remaining-patches-design.md) — updated after a 2026-05-09 decompile refresh to add a dynamic command-node tree before the remaining behavior slices. GT does not expose Scourge-style per-officer AI callbacks, but it does expose enough `Regiment` hierarchy and order-delay data for a Scourge-inspired generic command tree. Hard-coded `CorpsOrchestrator`/`DivisionOrchestrator`/`BrigadeOrchestrator` classes remain out; generic runtime command nodes are in. Recommended order: command-node tree, reserves, brigade stance, charge gate, line fallback, artillery prio. The original umbrella `2026-05-08-tactical-battle-orchestrator-design.md` is archived as historical reference.
- **Tactical orchestrator O2 + deployment observer integrated/deployed locally on `orch/o2-intent` (2026-05-09):** branch implements army intent inference + adversarial replan loop on top of O1 and now includes the parallel #58 tactical deployment observer work. O2 surface includes `TacticalIntentModel`, `EnemyVisibleState`, `ArmyIntentInference`, `ArmyTickCycle`, `ArmyEvidenceBuilder`, intent-aware `ArmyOrchestrator.Replan`, and runtime `[TacticalIntent]` / `[TacticalReplan]` telemetry. #58 adds default-on read-only `TacticalDeploymentObserverPatch` + `TacticalDeploymentTelemetry` around `BattleUnits.DoPlacementAIUnitsWithinDeploymentzoneNew` and `BattleUI.SetActiveDeploymentPhase`, emitting `[TacticalDeployment]`, `[TacticalDeploymentPhase]`, and `[TacticalDeploymentMove]` without changing unit positions, orders, formations, deployment zones, or orchestrator state. Combined console harness is **612 PASS / 0 FAIL** and `./build.sh` passes with 0 warnings / 0 errors. Corrected integrated `dist/WhiskeyRealism.dll` and deployed BepInEx plugin match SHA-256 `7d983391f14875c78ae79a6d4941230ba1d1296ab2f44ae952197b112d82f0b5` (733696 bytes; embedded PDB path `orch-o2-intent`). Now superseded locally by O3 DLL `2a9e1e2f…`. Pre-fix smoke proved `[TacticalIntent]` and `[TacticalReplan]` fire, but exposed the bad guessed field `BattleUnits.SideInformation.reservescommittedfraction`. Vanilla decompile confirms `SideInformation` has `averagemorale`, `totalactiveforce`, and `reinforcementarrivalswithin24hrs`, while reserve pools live at `AIBattle.objectivechain[*].reservegroups`; O2 now derives reserve commitment from those reserve pools instead of probing the nonexistent side-info field.
- **Slice B tactical runtime wiring plus HQ/follow bug guards merged/deployed locally (2026-05-08):** #48-#57 are implemented in `main`; final merged `dist/WhiskeyRealism.dll` and deployed BepInEx plugin match SHA-256 `a5a6e1fd099d11d2ff5dc6fd460d91e4e98a26a6f405df9d4b5dbfc808ed0d38` (663040 bytes). Console harness: **517 PASS / 0 FAIL**. `./build.sh` passed with 0 warnings / 0 errors. The B5 follow-up adds `TacticalOrderSettlementGate` and routes both the B5 writer and #35 matrix order-friction view through it, so queued vanilla orders, nonzero/unknown `orderstate`, interrupted paths, and active movement mode block Whiskey stance retasks. `TacticalSectorLedger` requires angle-slice contacts to meet a minimum enemy-strength/share gate before they can become decisive weak points. B5 maps defensive `AttackWeakPoint`, `Fix`, and `EconomyOfForce` sectors to stance 2 `defend-hold`, and the local stance writer is scoped to battle brigade groups only (`unittyp == 14`); division/army matrix rows report `command-scope` and stay vanilla/B6-owned. #53 targets private `Regiment.AddPath`; #54 fixes W&L operation null-before-transform cleanup; #55 clears newly-created cross-command group/HQ auto-links; #56 converts observed direct reserve support paths back into delayed `BattleUnits.SetWaypoint` orders; #57 is B6c reserve-list bias behind `Enable Tactical Reserve List Mutation`. C# defaults keep B4/B5/B7/B8/#53/#55/#56/#57 behavior writers default-off; #54 is default-on. Fresh runtime proof is still needed after restarting GTCW on the merged DLL for `[TacticalHqLinkGuard]`, `[TacticalReserveOrderDelayGuard]`, conditional `[Patch:WLOperationNullGuard]`, B5 queued/pending-order skips, tiny-contact weak-point suppression, `defend-hold` / `command-scope` rows, risky `[TacticalPathShape]` deltas, `[TacticalIntent]`/`[TacticalPlaybook]`, `[TacticalLocalReaction]`, `[TacticalReserveIntent]`, `[once:b8-microai-check-retreats]`, and conditional `[once:b7-cancel-bombard]`.
- **Historical operation doctrine implemented locally on `project-doctrine` (2026-05-06):** explicit catalog/no-profile CIC planning is default-on behind `Enable Historical Operation Doctrine`; dynamic phase-truth rules, operation identity persistence, operation replan memory, probe/package posture gates, and required #38/W&L/coastal/army-area hardening are in source. No catalog fallback chain was added: no profile is visible and `PickCampaignObjectivePatch` blocks vanilla random objective fallback while doctrine is enabled and CIC has no historical plan. Console harness and build passed; dist/deployed DLL hash matches `c90a5873e23ad1e7c0ac34e9c9b5cbad5554c0a5a2ee3fcc2aef299394366e0b` (481280 bytes). Runtime `[HistoricalOperation]` smoke is still pending; current behavior lives in `docs/historical-operation-doctrine.md`.
- **Coordinated operation packages built/deployed on `project-doctrine` (2026-05-06):** campaign-map attack/reinforcement packages are implemented through pure `CoordinatedOperationPackageLedger`, package-aware operational probes, W&L `Reinforce` intent, target-name resolution, and #38 `CoordinatedOffensiveOperationsPatch` + `CoordinatedOffensiveMicroMovementPatch`. Selected vanilla-offensive packages commit through `CoordinatedOperationRuntime`/`WlStrategicOrderBridge`, fail visibly with `package-no-commit` instead of silently falling back, preflight all selected units before side effects, reject non-atomic W&L current-order mixtures, and roll back direct defensive-moving-order/offensive-list/package-lock state on partial direct failures. Micro-movement filtering reinserts only package-locked units it removed, preserving unrelated vanilla removals. Runtime `[CoordinatedOps]` smoke is pending a fresh game launch/AI offensive opportunity; current behavior lives in `docs/coordinated-operation-packages.md`.
- **W&L dispatch bridge built/deployed on `project-doctrine` (2026-05-06):** #36 sanitizes stance-0 "none" dispatch text and `WlStrategicOrderBridge` routes eligible player-chain movement through vanilla current orders while failed/ineligible/player-involved W&L cases block direct movement. Current behavior lives in `docs/wl-dispatch-objective-bridge.md`.
- **Strategic project doctrine closed on `project-doctrine` (2026-05-06):** bounded Slice A economy enrichment is implemented, deployed, smoke-confirmed, and archived. #17 `ProjectSelectionPatch` is selection-only project doctrine; #39 `ProjectAppointmentObserverPatch` and #40 `ProjectUnlockObserverPatch` are observer-only. Do not mutate `IsAppointable`, `AppointProject`, `CheckProjectUnlocks`, `UseSubsidyForPurpose`, vanilla funding, or vanilla appointment behavior for this slice. Console harness and build passed; `dist/WhiskeyRealism.dll` and the deployed BepInEx plugin DLL match SHA-256 `f504f99d7266e0cdbf70e502a4b9f8d010990f3dde290c766d683040f653373d` (431104 bytes). Fresh campaign smoke confirmed project doctrine selection and unlock-observer wiring with no repeated exceptions; `[ProjectAppointed]` and `[ProjectUnlock]` remain conditional long-run telemetry.
- **Three parallel work streams merged into main on 2026-05-05** (`a40f1a5`; current local `origin/main` is `433cc5a` plus archived project-doctrine specs):
  1. **Strategic Resilience Director slice** — pure ledgers (`PhaseTruthLedger`, `ContactEvidenceLedger`, `CampaignPaceLedger`, `BattleHistoryQuery`, `TheaterPressureView`), `OffensiveAvailabilityWrapper` mirroring the 13 vanilla `IsUnitAvailableForOffensiveOperations` gates, `StrategicResilienceDirector` publishing per-alliance `DirectorPosture` with personality-clamped (±50% of personality delta) threshold modifiers, `DirectorPublishClamp` (one publish per real second), `DirectorMemory` sidecar persistence. Modulates probe options, transfer/hold thresholds, formation directive gates, fiscal/construction scoring, and defense budgets.
  2. **W&L Camp Realism Slice 1** — `#29 WlCampRealismPatch` corrects short-camp minimum undercrediting, retunes station 12 Rest from `6h/9h` to `3h/6h`, suppresses responsive weighting inside diary/event threshold scopes while keeping it for normal payoff/UI paths, softens command-count dilution for Drill/Motivate/Recruitment/Readiness, and gates early W&L `Diary.UpdateEvents` until the selected commander/current command and imported diary dependencies are ready.
  3. **Vanilla bug fixes** — `#28` economy NRE guard, `#30` commander previous-command cleanup, `#31` filter-map initialization guard/runtime tail guard, `#32` policy-36 state handover thresholding, `#33` AI fleet patrol reset, `#34` artillery combine gun preservation, `#37` `BattlefieldSetup.AssignFilters` economy filter-map bootstrap.
- **Merged main state:** console harness 265 PASS / 0 FAIL, build clean (0/0), merged DLL deployed and SHA-256 verified `5a8bf2c7116d60c5df56a6769911bc7daf99c0222df4a599f109f2b43e75521e` (380928 bytes). **First-fire smoke confirmed on 2026-05-05** (fresh CSA W&L 1861 campaign): `[CampaignPace]` posture publishing, `[Patch:TransferBudget]` / `[Patch:ArmyArea]` anti-zerg gating (zero `threat=asset:` lines), `[once:wl-camp-realism]` first-fire, `[Patch:CommanderAssignment]` / `[Patch:StateHandover]` / `[Patch:FilterMapInit]` (×2 distinct iterator signatures) / `[Patch:EconomyAllianceData]` bug-fix patches firing as designed, `[once:campaign-ai-paused]` governor active, zero exceptions or Harmony failures. Long-run/conditional-fire smoke (chapter 2+ Director cycling, camp-station behavior, fleet patrol / unit combine triggers) is open follow-up.
- **Perf delta on merged DLL:** `[DailyOps:Perf]` reports `formationMs` median **29-58 ms** (down from 240-290 ms baseline) — Director's typed-Regiment refactor + same-area unit bucket + WL camp branch's `CampaignAiUpdateGovernorPatch`/`FastForwardAiScheduler` tuning compose effectively. `defenseMs` ~165 ms remains as the next-largest hot path; pull only if 20×/50× speed feels slow during play.
- **Slice B tactical B0-B5 plus tactical bug-remediation state on `project-doctrine` (2026-05-07):** B0 observer smoke emitted all required `[Tactical*]` families and proved `[TacticalPlayerOrder] relation=ai-to-player-subordinate ... targetUnderCommander=True` with no tactical observer failures. B1 adds #41 `BattleChargeGatePatch` and #42 `BattleFeudActionGatePatch` behind default-off `Enable W&L Tactical Charge Guard`; B3 adds pure contact/sector/odds doctrine and `[TacticalOdds]`; B4 #44 and B5 #45 remain default-off in C# defaults because they write behavior-changing vanilla battle state (`macroai` / `ai_stance`), but the focused live config has them enabled. #35 `TacticalObserverPatch` now also has removable `[TacticalDecisionMatrix]` battle/group rows behind `Enable Tactical Decision Matrix Logging` plus row caps. Live smoke produced 37,250 matrix rows, 19 macro decisions, and 45 group decisions without exception spam, then exposed fixes now shipped: matrix W&L/order-friction input order corrected, no-measured-enemy sectors no longer become decisive weak points with absurd odds, B4 macro scoring now uses actual `AIBattle.unitsused` contact/sector evidence instead of force-balance/no-sector inference, B5 explicit Probe sectors can still screen/probe when not blocked by command scope or defensive macro, B5 now skips `macroai == 3` so vanilla owns retreat stance, B5 skips unsettled vanilla order queues/orderstate instead of stacking stance orders, tiny angle-slice contacts no longer drive decisive weak-point attacks, B5 defensive weak-point/fix sectors hold under `macroai == 2`, and B5 no longer writes local stance to division/army groups. No-contact tactical odds are zero. #46 `BattleObjectiveChainWlGuardPatch` filters protected objective-chain entries during `AIBattle.UpdateMovingTargets`, then restores them after vanilla runs. This older tactical stack is superseded by later orchestrator and terrain/facing feature work; historical plans live under `docs/superpowers/plans/archive/`. Current tactical living state is in `docs/handoff.md`, `docs/patch-catalog.md`, `docs/bug-fixes/vanilla-tactical-battlefield.md`, `docs/tactical-orchestrator.md`, and `docs/tactical-terrain-facing-discipline.md`.
- **B6b tactical local-reaction scorer implemented on `feature/tactical-b6b` (2026-05-07):** pure `TacticalLocalReactionScorer` and `TacticalReservePolicyLedger` are implemented with console harness and `./build.sh` passing. B6b adds no Harmony patches, config, deploy, or in-game smoke gate. Downstream contract: `PermitCharge` is the only positive charge permission; shipped B6b does not emit `DenyCharge` or `LocalFallbackPressure`, so B6c must treat non-`PermitCharge` as charge denial and B8 must derive or add fallback pressure before withdrawal behavior.
- Default-off telegraph AI (#24) still needs a focused enabled smoke run; that's an opportunistic Slice A follow-up, not a blocker.
- Bug Fixes now has a durable workstream at `docs/bug-fixes/`. The active queues are `docs/bug-fixes/vanilla-ai-economy.md`, `docs/bug-fixes/vanilla-tick-system.md`, `docs/bug-fixes/vanilla-hotfix-parity.md`, and `docs/bug-fixes/vanilla-tactical-battlefield.md`: `BUG-ECO-001` has regression coverage for the runtime-confirmed fiscal subsidy sentinel leak; `BUG-ECO-003` has #28 guard code for repeated `Economy.UpdateEconomyAllianceData` Player.log NREs; `BUG-TICK-001` extends #26 with pause/high-speed AI governor behavior; `BUG-TICK-002` has #31 bounded filter-map initialization/runtime tail guard and #37 pre-`AssignFilters` economy filter-map bootstrap after Player.log showed `BattlefieldSetup.AssignFilters` throwing before `productionmapsymbols` creation; `BUG-TICK-003` has #29 W&L diary-startup gate after Player.log showed early `Diary.UpdateEvents` NREs under `BattleUnits.Update`; Community Hotfix parity has #30 commander previous-command cleanup, #32 policy-36 state handover thresholding, #33 AI fleet patrol reset, and #34 artillery combine gun preservation; Tactical battlefield has telemetry-first `BUG-TAC-*` findings plus default-off #43 fallback/retreat NRE containment. The current deployed local DLL is the tactical probe/formation follow-up build listed above; #37 fresh-launch smoke confirmed one bootstrap marker and no `BattlefieldSetup.AssignFilters` / `Economy.UpdateFilterMaps` recurrence through 1861-06-17, #29 diary-startup smoke confirmed one expected skip line with no `Diary.UpdateEvents` NRE recurrence, and focused tactical smoke confirmed only `BUG-TAC-005` exposure. Policy null-personality, supply-depot construction, railroad construction, and most tactical `BUG-TAC-*` items remain repro/backlog items.
- Slices C (W&L hierarchy AI) and D (additional historical flavor) remain deferred.

## Load-Bearing Runtime Lessons

- W&L command selection is fragile. Vanilla invokes `CareerInformationPanel.ShowStartUnitSelectionList(true)` at campaign frame 50, then pauses. Do not invoke it before `GameVars.frame >= 50`.
- A visible `UnitSelectionListObject` is not enough. The picker is usable only when private `unitlineappointcommand` rows exist.
- Existing BepInEx config values override C# defaults after first plugin load; changing `Plugin.cs` defaults alone does not update an existing user's config.
- DLL-affecting changes are not ready until built, deployed, and verified by matching `sha256sum` between `dist/WhiskeyRealism.dll` and the BepInEx plugin DLL.
- Harmony patches must not mutate strategic mod state. State writes happen through coordinator/daily/event paths; patches steer vanilla decisions and log bounded evidence.
- Defense Intent Ledger (2026-05-04) migrated the operational tick from weekly to daily; `EmergencyExitStableTicks=14` rescale and `FrontSectorRuntime.Signature` bucket coarsening make daily safe from thrash.
- Campaign-stutter diagnosis on 2026-05-05 found vanilla 20x/50x AI as the baseline hotspot: `AICampaign.Update` runs `floor(sqrt(GameVars.gamespeed))` `UpdateUnitAI()` passes per rendered frame and a live 20x frame measured `26.35 ms` before Whiskey extra catch-up. Current deployed code compacts default `[DefenseIntent]` logs, caches #25/AICampaign/#4 reflection, de-jitters daily strategic review (`front`/`fiscal`/`defense` daily; `formation` alternating/forced; `army-area` and construction full scans weekly/source-driven), enables #26 `CampaignAiUpdateGovernorPatch` by default to cap vanilla high-speed passes while preserving vanilla side effects, removes reflection from `DefenseIntentRuntime.GetXZDistance()`, uses typed `Regiment` reads in defense hot loops, and resolves `Frontline2` once per defense pass. The #21 catch-up patch stands down while #26 is enabled. The shipped performance plan is archived at `docs/superpowers/plans/archive/2026-05-05-campaign-ai-performance-governor.md`.
- `BattlefieldSetup.AssignFilters` runs at campaign frame 29 and assumes all economy filter maps already have textures; `BattleUnits.CampaignDataRuns` does not run until frame 31. If W&L startup reaches `AssignFilters` before `Economy.UpdateFilterMaps` has created those maps, vanilla throws and never creates `BattlefieldSetup.productionmapsymbols`, causing later `Economy.UpdateFilterMaps` full-cycle resets to NRE at `productionmapsymbols.ResetValues()`. #37 mirrors vanilla map initialization before `AssignFilters`; #31 remains the bounded tail guard and now logs production-symbol health if it still fires.
- `Diary.UpdateEventQueue` starts calling `Diary.UpdateEvents` at campaign frame 50, but `UpdateEvents` assumes W&L commander/current-command, imported diaryevents, cardinal points, food, and weather are ready. If W&L startup has not selected a live command yet, vanilla can throw early under `BattleUnits.Update`. #29 now skips only W&L diary event updates until those dependencies are ready, then falls through to vanilla; do not convert this into broad diary exception suppression.
- Fort-spam diagnosis on 2026-05-05 found vanilla `AICampaign.CheckFortConstruction(int)` has one active fort order per faction and a nearby-fort spacing check, but no area/capital cap once prior orders finish; capital-defense units get a looser `0.6x` spacing multiplier. #27 `FortConstructionGovernorPatch` now filters saturated `fortconstructionsites` per vanilla call, using local fort/order counts plus local enemy threat. Details live in `docs/fort-construction-governor.md`.
- `DefenseCooldownTable._recoveredStarted` is an idempotency guard: set it on the same tick that the cooldown entry transitions from recovering → active to prevent double-triggers across multiple daily ticks.
- `CampaignMapLedger` is now the active-map source for towns, represented states, forts, sea harbors, and river harbors. Do not reintroduce hardcoded Mississippi/Alabama assumptions for W&L unless the runtime map actually exposes those states.
- #4 capital-defense add-on should stay proportional: readiness-gated, morale-adjusted, and penalizing gross overmatch for small threats.
- Strategic anti-zerg doctrine is now load-bearing: `AssetProximity` is observation/local pressure, not an invasion movement trigger; theater/front budgets decide whether units can export; cross-map defense requires a real national emergency; capital defense uses a capped package; and release/return flows must not strand units or strip other theaters.
- Operational probe doctrine is now load-bearing: a strategic plan may test an area with one bounded same-area formation before mass commitment; enemy reaction should pause instead of pulling armies from other theaters; favorable contact can escalate; probes must never draw from critical hold sectors; and tempo must vary by vanilla chapter/Whiskey era/season/faction/personality so 1861 probing is slower, 1864 Union pressure is more sustained, late CSA commitment is more conservative, and winter operations slow down.
- `AICampaign.aifaction` can include alliance 2 (Europe). Per-alliance arrays sized to Union+CSA (length 2) must bound-check against `arr.Length` before indexing; `AICampaignReflect.GetAllianceId(_aifaction)` returns the underlying alliance, which can be 2. Slice 2 patches (#25 + custom-order runner) early-return for `allianceId > 1` rather than indexing.
- Test project (`tests/WhiskeyRealism.Tests/WhiskeyRealism.Tests.csproj`) uses explicit `<Compile Include>` entries per source file — there is no glob. Adding a new strategic file requires a matching csproj entry; deleting one requires removing the entry. Build will succeed against stale entries until the file is actually consumed by a test.

## Current Priorities

1. **Next workstream:** run focused Active smoke on the final merged `main` DLL for the full-spectrum tactical command doctrine. Final build/deploy/hash verification is complete at SHA-256 `969f1aec126057ac8453cefbee212a099db2eaef277a3c2d35490a47b0125897` (933376 bytes). The current local config explicitly sets `Tactical Commander Mode = Active`, `Enable Tactical Battle Orchestrator = true`, `Enable Tactical Orchestrator Army = true`, `Enable Tactical Orchestrator Intent Inference = true`, `Enable Tactical Orchestrator Reserve Commit Gate = true`, `Enable Tactical Orchestrator Direct-Child Gate = true`, `Enable Tactical Orchestrator Charge Gate = true`, and `Enable Tactical Decision Matrix Logging = true`. Watch for `[TacticalCommanderMode] mode=Active`, `[TacticalOpsLedger]`, `[TacticalCommandAssignment]`, `[TacticalCommandPosture]`, `[TacticalPostureSummary]`, `[TacticalReserveDrift]`, `[TacticalGroupDecision]`, `[TacticalDoctrineCharge]`, `[TacticalOrchestratorChargeGate]`, bounded B8 reserve/fallback rows, no repeated `Exception`/`ERROR`/`missing-anchor`/Harmony failures, no player-side or player-subordinate retasking, no non-reserve illegal-idle command left without a ledger reason, no skirmisher-only main-line assault, and no doctrine-denied reserve movement left as an active direct path. Living tactical status is in [`docs/tactical-orchestrator.md`](docs/tactical-orchestrator.md), [`docs/tactical-operations-ledger.md`](docs/tactical-operations-ledger.md), and [`docs/tactical-terrain-facing-discipline.md`](docs/tactical-terrain-facing-discipline.md). Do not archive the full-spectrum doctrine spec/plan until this smoke passes.
2. **Remaining focused tactical smoke:** after operations-ledger Active proof, run Slice 1 reserve gate, Slice 3 charge gate, and #60 tactical terrain/facing smoke on the latest deployed DLL. Watch for bounded `[TacticalOrchestratorChargeGate]`, `[TacDeployTerrain]`, and `[TacDeployTerrainAdvice]`; terrain/deployment-failure-only corrections; no player-side/player-subordinate corrections; no repeated exceptions; no #53 pathfinder regression; no #41/#53/#55/#56/#58/#59/#60 warning; and no O3/command-tree fail-open exception. Remaining tactical-orchestrator behavior consumers still include Slice 2 brigade stance, line fallback, and artillery prio.
3. **Remaining tactical proof:** B2/bug telemetry has been smoke-sampled. Next tactical bug-fix proof is `BUG-TAC-005` path/position delta evidence after `[TacticalObjectiveMove] risk=True`, plus any risky `[TacticalCourierQueue]`, campaign-call `[TacticalCurrentOrder]`, `[TacticalWaypointDrift]`, `[TacticalReserveMove]`, `[TacticalChargeGuard]`, `[TacticalFeudGuard]`, or `[Patch:TacticalFallbackRetreatNullGuard]` lines if those paths finally exercise. Watch for `missing-anchor`, `failed-owned`, `TargetInvocationException`, Harmony failures, or repeated exceptions.
4. **Strategic runtime smoke**: in a separate fresh campaign run, confirm bounded `[HistoricalOperation] action=select` or visible `action=no-profile` / `skip-vanilla-random` lines, plus `[CoordinatedOps]` only when a real offensive opportunity appears. Do not claim in-game doctrine/package fire until that fresh log exists.
5. Slice A follow-ups (opportunistic, none blocking):
   - Slice 3 guard-budget tuning from runtime telemetry — adjust `GuardBudgetFraction` (default 0.10), `cooldownDays` (default 4), and aggregator thresholds (0.75 / 1.0 / 1.25) per faction/era from observed telemetry.
   - `AssetRoleCatalog` refinement from non-East scenarios — current ~50 entries cover the W&L East-coast smoke; look for `[DefenseIntent:asset] missing-role` lines in CSA Western/Trans-Miss campaigns.
   - Optional telegraph AI smoke with `Enable Telegraph AI = true`.
   - Supply-depot/railroad construction doctrine only after observer data proves the gap. Fort saturation guard #27 has shipped; future fort work should be tuning from runtime `[Patch:FortGovernor]` evidence, not a new broad construction rewrite.
6. Keep Slices C (W&L hierarchy AI) and D (additional historical flavor) deferred unless the user explicitly redirects.

## Update Rules

- Update this file only for durable state that should survive compaction, handoff drift, or fresh agent sessions.
- Do not paste long logs here. Summarize the decision and link to the authoritative file or commit.
- If this file conflicts with shipped code or `docs/patch-catalog.md`, the shipped code/catalog wins and this file should be corrected.
- Keep this file repo-safe: no secrets, tokens, local-only account details, or unredacted private logs.
