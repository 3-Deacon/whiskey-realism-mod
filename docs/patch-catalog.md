# Patch Catalog

Canonical numbered catalog of all shipped Harmony patches. Each item has a stable ordinal (never re-used even after withdrawal) and a one-line description with file path + decompile coordinates.

| # | Patch | Type | File | Targets (decompile line) | Description |
|---|---|---|---|---|---|
| 1 | `PickCampaignObjectivePatch`   | Prefix  | `Patches/PickCampaignObjectivePatch.cs` | `AICampaign.PickCampaignObjective` (17769)   | Replace vanilla random pick with active CIC plan target; vanilla fallback when no plan / player-CIC. |
| 2 | `ImportanceValuesPatch`        | Postfix | `Patches/ImportanceValuesPatch.cs`      | `AIArea.CalculateMostValueableAIZones` (10964) — *target changed in v0.2.1; original v0.2.0 target was AICampaign.UpdateImportanceValues which had wrong shape* | Override `__instance.mostvalueableaiareaclose[aifaction]` to point at the plan-target AIArea. Resolves CampaignObjective ID → first Town/IIP position → AICampaign.aiareas.GetColorOnPos → AIArea.GetAIArea. |
| 3 | `TransferOfUnitsPatch`         | Prefix/Postfix | `Patches/TransferOfUnitsPatch.cs` | `AICampaign.CheckTransferOfUnits` (17232) | v0.2.2 concrete steering. Temporarily redirects vanilla `positiondeficit` to the active plan target when the target is under-strength, after consulting `FrontSectorLedger` so critical/front-hold sectors are not hollowed out. Logs `[once:transfer]`, bounded `[Patch:TransferBudget]` block/concession lines, and `[Patch:Transfer]` queue lines. |
| 4 | `DefensiveOpsPatch`            | Postfix | `Patches/DefensiveOpsPatch.cs`         | `AICampaign.AssignUnitToDefendCapital` (11668; strength gate 11791) | v0.2.2 concrete steering. After vanilla capital-defense assignment, applies CIC personality and `CampaignMapLedger` capital value to the capital-defense strength gate, then can pledge one extra eligible defender while preserving vanilla movement/order execution for the next pass. Candidate choice is proportional: readiness-gated, morale-adjusted, and penalizes pulling a grossly oversized army for a small capital threat. Reflected vanilla helper lookups are cached, including the nested `AICampaign.FortConstructionOrder.UnitAlreadyConstructing(Regiment, FortConstructionOrder)` signature. Logs `[once:defensiveops]` and bounded `[Patch:DefensiveOps]` assignment lines. Build/deploy verified; first-fire smoke confirmed. Assignment lines are conditional. |
| 5 | `BattleResultObserverPatch`   | Postfix | `Patches/BattleResultObserverPatch.cs` | `BattleMonument.UpdateAllianceWon` (76496) | v0.2.2 observer. Records final land-battle outcomes into `StrategicCoordinator.BattleHistory`, persists the ring buffer, and marks both factions dirty for weekly succession/plan reevaluation. Build/deploy/smoke verified. |
| 6 | `CommanderReplacementPatch`    | Prefix  | `Patches/CommanderReplacementPatch.cs`  | `AICampaign.CheckAICommanderReplacements` (17008) | v0.2.1 concrete swap — when scheduler has un-applied scripted events for this faction, resolves replacement Commander by `combinedname` last-token + alliance, finds an army-group commander (`IsArmyGroupCommander() && currentcommand != null`) to displace, calls `AssignCommando` + `DoCommanderPromotion`, marks applied (persisted via sidecar). Returns false to skip vanilla on apply; falls through if preconditions unmet. |
| 7 | `PerkSelectionPatch`           | Prefix  | `Patches/PerkSelectionPatch.cs`         | `AICampaign.CheckPerkSelection` (11871)      | Post-v0.2.2 concrete steering. Replaces vanilla random campaign army/fleet perk picks with role-aware scoring, mirrors vanilla W&L player-subordinate army skip, and still delegates final assignment to `Regiment.ChoosePerk` so empty-slot, duplicate, XP reset, and side effects remain vanilla-owned. Logs `[once:perks]` plus bounded `[Patch:Perks]` role/perk signatures. |
| 8 | `RecruitmentPatch`            | Prefix/Postfix | `Patches/RecruitmentPatch.cs`          | `AIArea.GetBestRecruitingState` (10722), scoped by `AICampaign.ZoneRecruiting` (12405) | v0.2.2 recruitment state steering. While vanilla `ZoneRecruiting` is already active, replaces the chosen recruiting state only when a `RecruitmentIntentLedger` candidate clears vanilla support/recruit-pool gates and better matches the active plan theater. Post-release main adds threatened-priority-area protection: CSA `VirginiaCapitalCorridor` and Union `WashingtonDefenses` can override broader plan-theater steering only when formation-local enemy pressure crosses the threat threshold and an eligible protected-area state exists. Does not alter unit type, group creation, manpower spending, or raid/sea-invasion recruitment calls. Logs `[once:recruitment]` and bounded `[Patch:Recruitment]` replacement lines. |
| 9 | `MonthlyTickHookPatch`         | Postfix | `Patches/MonthlyTickHookPatch.cs`       | `AICampaign.Update` (11159)                  | Drives `StrategicCoordinator.NotifyDateAdvanced` from per-frame Update; reads day/month/year, caches reflection fields, suppresses unchanged same-day callbacks, and allows one same-day callback when vanilla `AICampaign.aifaction` first appears. CIC heartbeat stays immediate; operational ledgers defer until vanilla AI factions exist. First-fire marker is `[once:strategic-cadence]`; startup wait is logged once. Hot-path reflection is cached so the cadence hook does not re-resolve readiness fields every frame. |
| 10 | `CampaignParametersLockPatch` | Postfix | `Patches/CampaignParametersLockPatch.cs` | `MainMenu.SetCampaignParameters` (193675)   | Override `usedcampaignagressiveness` → 1.0 and `usehistoricaipersonality` → true after vanilla writes player menu picks. Final value-lock; difficulty intentionally untouched. Gated by `OverrideVanillaSettings` config (default true). |
| 11 | `AggressivenessSliderLockPatch` | Postfix | `Patches/AggressivenessSliderLockPatch.cs` | `MainMenu.SwitchAIMode(float)` (~193739) | UI grey-out for campaign-aggressiveness slider. Snaps `aimode = 1.0`, sets displayed text to "Aggressiveness: Locked by Whiskey Realism". Gates by gameObject name to avoid touching BattlePanel's per-battle "AI Mode" toggle. |
| 12 | `HistoricCheckboxLockPatch`   | Postfix | `Patches/HistoricCheckboxLockPatch.cs`  | `MainMenu.CheckForCheckBoxUpdates` (193612) | UI grey-out for Historic/Dynamic radio. Forces CheckBoxes[0]=true, [1]=false, syncs `lastcheckboxstates`, reverses `ChooseHistoricPolicies(false)` calls if player tried to flip to Dynamic. Calls `CheckBox.Freeze(true)` for visual half-alpha + click-block. Reflection is cached and already-locked state returns early to avoid setup-menu lag. |
| 13 | `DifficultySliderLockPatch`  | Postfix | `Patches/DifficultySliderLockPatch.cs`  | `MainMenu.ChangeBonus(float)` (~193786)     | UI grey-out for campaign-difficulty slider. Snaps `bonus = maxstartbonuscampaign × (LockedDifficulty/4)` (default index 3 = "Hard"); overwrites `bonustext` to `"Locked:Realism"`. Gated by `gameObject.name != "BattlePanel"` to leave per-battle difficulty alone. |
| 14 | `RealismCheckboxesLockPatch` | Postfix | `Patches/RealismCheckboxesLockPatch.cs`  | `MainMenu.CheckForCheckBoxUpdates` (193612) | Forces 5 realism CBs ON + frozen: `FogOfWarCB`, `OrderDelaysCB`, `FeudsCB`, `FullReadinessCB`, `AllAutomanageCB`. Belt-and-suspenders writes the underlying `GameVars.usefow=true`, `useorderdelays=true`, `debug_deactivatefeuds=false`, `fullreadiness=false`, and calls `MainMenu.SetAutomanage(true)` directly. Reflection is cached and already-locked state returns early to avoid setup-menu lag. |
| 15 | `ArmyAreaTheaterPatch`       | Postfix | `Patches/ArmyAreaTheaterPatch.cs`       | `AICampaign.UpdateCampaignTheaters` (17034) | v0.2.2 historical operating-area steering. After vanilla updates `Regiment.theaterposition`, idle AI top formations, including independent top divisions, whose `ArmyAreaLedger` assignment says they are outside their historical operating area are nudged back via vanilla `AICampaign.MoveUnitTo` unless the formation directive blocks direct movement. `ArmyAreaRuntime` now writes `theaterposition` only after `MoveUnitTo` succeeds. Unknown dynamic commands receive a local active-map doctrine from current area and adjacent areas; known off-map full-war army names remain unassigned instead of pretending their theater exists in W&L. Logs `[once:army-area]`, non-spammy missing-ledger warnings, and bounded `[Patch:ArmyArea]` return-area lines. First-fire smoke confirmed. |
| 16 | `ArmyGroupManagementPatch`   | Postfix | `Patches/ArmyGroupManagementPatch.cs`   | `AICampaign.CheckArmyGroupManagement` (17705) | v0.2.2 historical army-group steering. After vanilla army-group management, committed top formations sharing a historical operating command are attached to an existing ArmyGroup or used to create one through vanilla `ArmyGroup` APIs; directive-qualified independent divisions can attach but cannot seed a division-only group. Preferred historical commanders are appointed only if already in that grouped command or currently unassigned. Logs `[once:armygroup]`, non-spammy missing-ledger warnings, and bounded `[Patch:ArmyGroup]` create/attach/appoint lines. First-fire smoke confirmed. |
| 17 | `ProjectSelectionPatch`      | Prefix | `Patches/ProjectSelectionPatch.cs`      | `AICampaign.UpdateProjects` (17487) | Project doctrine selection steering. Uses `ProjectDoctrineCatalog` / signals / scorer / `ProjectDoctrineLogGate` to replace queued AI project choices only when doctrine beats vanilla by margin; vanilla still owns funding and appointment. Suppresses fully broken project 98, keeps partially broken project 107's civil-order/casualty value without raiding scoring, applies date-window replacement penalties and half-funded hysteresis, skips lanes 6/7, treats construction-winning lanes as observe/log only, and reads only cheap runtime signals: blockade ratio, intervention probability, own project levels, and enemy project levels. Logs `[once:project-selection]`, `[once:project-doctrine-catalog]`, `[once:project-doctrine-selection]`, `[once:project-doctrine-starved-lane]`, and bounded `[ProjectDoctrine]` lines. Build/deploy/hash verified in local branch DLL `f504f99d7266e0cdbf70e502a4b9f8d010990f3dde290c766d683040f653373d`; fresh campaign smoke on 2026-05-06 confirmed catalog/selection first-fire and `[ProjectDoctrine]` replacements for both alliances with no repeated exceptions. |
| 18 | `FinancialAIPatch` | Postfix | `Patches/FinancialAIPatch.cs` | `AICampaign.UpdateFinancialAI` (15352) | Fiscal intent correction after vanilla bond/tax/subsidy logic; bounded to at most two lane moves and respects player automanage gates. |
| 19 | `PolicySelectionPatch` | Prefix | `Patches/PolicySelectionPatch.cs` | `Policies.CheckAIPolicyChange` (211015) | Fiscal + grand-strategy policy steering. Falls through for player-managed and emergency personalities; starts policy research only on a clear score win, preserves fiscal force/cost suppression as dominant, and biases Union blockade setup plus CSA cotton/free-trade/blockade-running/diplomacy timing. Logs one `[Patch:PolicySelection]` line only when it actually starts, replaces, or blocks policy research. |
| 20 | `EconomyConstructionPatch` | Prefix | `Patches/EconomyConstructionPatch.cs` | `AICampaign.UpdateCompanyFoundations` (15000) | Private-building construction steering. Biases existing vanilla `bestiipplacesprob` candidates before vanilla selection, combining fiscal multipliers with `ConstructionIntentLedger` private-building intent when site steering is enabled. Preserves vanilla-selected `bestiipplaces` and vanilla funding/rating/ownership/placement/upgrade/execution gates; does not replace IIPs or call `AddConstructionWish` directly. Per-candidate guard prevents repeated multiplication between candidate refreshes. |
| 21 | `FastForwardAiCatchUpPatch` | Prefix/Postfix | `Patches/FastForwardAiCatchUpPatch.cs` | `AICampaign.Update` (11159), private `AICampaign.UpdateUnitAI` (11304) | Fast-forward campaign AI catch-up. At 20x/50x, runs extra vanilla `UpdateUnitAI` job passes only when the current high-speed AI frame is still cheap; slow vanilla or extra passes trigger an adaptive cooldown. Automatically stands down when #26 `CampaignAiUpdateGovernorPatch` is enabled so high-speed AI scheduling has one owner. Logs `[once:fast-forward-ai]`, non-spammy `[Patch:FastForwardAI]` samples, and bounded `[Patch:FastForwardAI:Perf]` diagnostics showing vanilla/extra timing when throttled. |
| 22 | `WlCareerStartSelectionRetryPatch` | Postfix | `Patches/WlCareerStartSelectionRetryPatch.cs` | `AICampaign.Update` (11159), `CampaignController.Update` (29840), calls `CareerInformationPanel.ShowStartUnitSelectionList(bool)` (182353) | W&L startup safety net. Vanilla shows the command-selection popup only once at campaign frame 50; if that one-shot races panel/list readiness, the campaign remains paused with no usable command picker. While `WlCareerStartGate` says the player has no command, this patch retries the vanilla popup call from `CampaignController.Update` using Unity-frame throttling only after `GameVars.frame >= 50`. `CampaignController.Update` is the primary retry anchor because it owns the `careerinformationpanel` field; `AICampaign.Update` remains a fallback but cannot consume attempts before the panel exists. It stops only once the unit-selection list is visible **and** has selectable `unitlineappointcommand` rows, or once the player has a command. Logs are bounded: first-fire `[W&LStartSelection]`, one waiting line below frame 50, one visible-empty line if the outer panel is up without rows, one visible-pending line when rows exist, and retry status including `linesBefore/linesAfter`. Do not reintroduce pre-frame-50 retries; runtime evidence showed they can create a half-open picker and leave the campaign unable to advance time. |
| 23 | `ConstructionObserverPatch` | Postfix | `Patches/ConstructionObserverPatch.cs` | `CBuilding.Place` (96163), `BattleUnits.Railroad.StartConstruction` (77818) | Construction telemetry observer. Records actual building and railroad starts for comparison against `ConstructionIntentLedger`; does not alter construction, placement, funding, unit eligibility, or railroad selection. |
| 24 | `TelegraphConstructionRuntime` | Coordinator/helper/runtime | `Strategic/Construction/TelegraphConstructionRuntime.cs` | weekly `StrategicCoordinator.UpdateConstructionIntent`; validation hooks on `CBuilding.Place` (96163) and `CBuilding.WorkDownConstructionWishes` | Optional conservative telegraph AI. Default OFF behind `EnableTelegraphAI`; starts only connected-chain telegraph candidates, validates support-unit eligibility and final placement, cleans cancelled pending placements, and caps active work by `Max Active Telegraph Constructions Per Faction`. |
| 25 | `CheckForDefensiveOperationsCandidateFilterPatch` | Prefix + Postfix | `Patches/CheckForDefensiveOperationsCandidateFilterPatch.cs` | `AICampaign.CheckForDefensiveOperations` (13505) | Snapshots `aifaction[i].ownunits`, removes ledger-forbidden references (`forbidden-cross-map`, `critical-front`, `asset-proximity-local-only`, `national-emergency-required`, `formation-directive`, `formation-donor`, `min-hold`, `critical-sector-budget`, `player-controlled`) for the duration of vanilla's call, restores the full list in Postfix, and reverts any forbidden pull vanilla committed despite filtering. Revert walks `unitsindefensiveoperations` for new additions, removes via cached `DefensiveOperation.RemoveUnit` reflection, and calls `MoveUnitTo` back to the unit's pre-Prefix `theaterposition` unless W&L player-chain/player-controlled semantics require skipping direct movement. Revert deduplication via `_revertLogged` (alliance, threat-sig, unit-id) prevents daily spam on vanilla's secondary attack-op branch. Defense intent slice 2 enforcement surface 1. |
| 26 | `CampaignAiUpdateGovernorPatch` | Prefix | `Patches/CampaignAiUpdateGovernorPatch.cs` | `AICampaign.Update` (11159), private `InitializeAI` (11204), private `UpdateUnitAI` (11304), private `UpdateCompanyFoundationList` (15082), `CBuilding.WorkDownConstructionWishes` (97538) | Default-on campaign AI tick governor. Skips post-initialization paused updates so vanilla does not run a `Mathf.Max(1, sqrt(0))` `UpdateUnitAI` pass while time is stopped. At 20x/50x, replaces vanilla pass-count scheduling with a bounded wrapper that delegates to vanilla private methods, preserves debug-off and initialize-and-return behavior, falls through to vanilla when AI debug map text is enabled, and always preserves company-foundation plus construction-wish side effects. Defaults cap 20x from 4 to 2 passes and 50x from 7 to 3 passes. Logs bounded `[Patch:CampaignAIGovernor]` samples when it caps, budget-stops, or first sees the paused guard. |
| 27 | `FortConstructionGovernorPatch` | Prefix/Postfix/Finalizer | `Patches/FortConstructionGovernorPatch.cs` | `AICampaign.CheckFortConstruction` (16347) | Default-on fort-spam governor. Temporarily filters vanilla `fortconstructionsites` for the current faction/unit call, suppressing saturated local areas unless local enemy threat justifies more works and hard-capping capital clusters even under threat. Restores the vanilla site list after the call and preserves vanilla unit eligibility, one-active-order-per-faction behavior, movement orders, fort placement search, and construction-wish side effects. Logs bounded `[Patch:FortGovernor]` suppressions. |
| 28 | `EconomyAllianceDataGuardPatch` | Finalizer | `Patches/EconomyAllianceDataGuardPatch.cs` | `Economy.UpdateEconomyAllianceData` (32344; current Player.log NRE offset `[0x00a30]`) | Vanilla bug guard for repeated economy filter-map NRE spam. Suppresses only `NullReferenceException` from the vanilla alliance-data aggregation method so `Economy.UpdateFilterMaps` can advance its IIP/corporate/town iterators instead of logging the same stack every frame. Logs bounded `[Patch:EconomyAllianceData]` with iterator state. Build/deploy/hash verified; fresh-launch smoke pending. |
| 29 | `WlCampRealismPatch` | Prefix/Postfix/Finalizer | `Patches/WlCampRealismPatch.cs` | `Camp.EvaluateCampTime` (172034), `Camp.Station.GetCurrentBonus` (171473), `Camp.GetModifier` (172522), `Camp.Station.CheckEventTriggers` (171534), `Diary.UpdateEvents` (182902) | W&L camp realism Slice 1. Corrects vanilla short-camp minimum allocation by replacing only the latest station-history credit entries, refreshes camp status before siege/field caps when safe, caps station 12 Rest from vanilla `6h neutral / 9h full reward` to `3h neutral / 6h full reward`, adds responsive bonus weighting for safe station payoff/UI paths while suppressing that responsive weighting inside diary/event threshold scopes, softens command-count dilution for Drill/Motivate/Recruitment/Readiness only, and gates W&L `Diary.UpdateEvents` during startup until the selected commander record/current command plus imported diary events/cardinal points/food/weather-on-cycle-1 dependencies are ready. Build/deploy/hash verified in local DLL `9d4c12fce45ac8fa0c07adca14903fc3641aadee3732968c717e94f3482d8d82`; fresh-launch diary smoke confirmed. |
| 30 | `CommanderAssignmentPreviousCommandPatch` | Prefix/Postfix | `Patches/CommanderAssignmentPreviousCommandPatch.cs` | `GameVars.Commander.AssignCommando` (60343) | Vanilla bug guard for stale/duplicate commander ownership. Snapshots the newly assigned officer's previous `currentcommand` before vanilla assignment, then clears that prior unit's `commander` field if it still points at the same commander after the new command is assigned. Skips that clear when `AssignCommando` is executing inside vanilla `BattleUnits.ReplaceCommanderOfUnit`, because vanilla still reads the subordinate `commander` id later in that same method. Preserves vanilla target-unit old-commander removal and naming side effects. |
| 31 | `EconomyFilterMapInitializationGuardPatch` | Prefix/Postfix/Finalizer | `Patches/EconomyFilterMapInitializationGuardPatch.cs` | `Economy.UpdateFilterMaps` (31852), protecting `BattleUnits.CampaignDataRuns` unbounded loop (79874) and runtime `BattleUnits.Update` calls (79341) | Vanilla bug guard for campaign-start economy initialization hangs and runtime filter-map NRE spam. If `UpdateFilterMaps(initialization:true)` repeatedly returns false without iterator progress, or throws during initialization, forces that one startup loop complete with bounded warning. For runtime `UpdateFilterMaps(false)`, suppresses only plausible-state `NullReferenceException`, advances the town/IIP/corporate cursor using vanilla tail semantics, emits bounded cursor/object diagnostics for root-cause follow-up, and returns false so the next frame does not retry the same poisoned map item. |
| 32 | `MilitiaActStateHandoverGuardPatch` | Prefix/Finalizer + Prefix | `Patches/MilitiaActStateHandoverGuardPatch.cs` | `Policies.LaunchPolicyEffect` policy 36 handover block (210050), `GameVars.SetNationToNewAlliance` (66850) | Vanilla bug guard for first-militia-act state ownership flips. While policy 36 is applying its chapter-1 handover, blocks USA/CSA state transfers unless the receiving side has decisive support (`>=65%`) and the opposing side is at or below `35%`. Does not affect objective/scripted state transfers outside that scoped policy effect. |
| 33 | `FleetPatrolResetPatch` | Prefix | `Patches/FleetPatrolResetPatch.cs` | `Regiment.StopRegiment(bool, bool, bool, bool, bool, bool)` (132316), patrol restore branch (132392) | Vanilla bug guard for AI fleet patrol loops. Before `StopRegiment` restores `fleetorders == 2` patrol waypoints, resets non-player idle fleets at their patrol start point to normal orders so `AICampaign.CheckFleetMovements` can select them for new operations. Player patrol orders are untouched. |
| 34 | `ArtilleryCombineGunTransferPatch` | Prefix/Postfix | `Patches/ArtilleryCombineGunTransferPatch.cs` | `BattleUnits.CombineUnits` (93153) | Vanilla bug guard for artillery unit combinations. When artillery crew strength is merged into a target unit, adds the proportional source organic guns to the receiving unit and recalculates gun stats without battle-loss side effects. Preserves vanilla strength, sick/loss, weapon-stock, and source-delete behavior. |
| 35 | `TacticalObserverPatch` | Postfix observers + Prefix/Postfix snapshots | `Patches/TacticalObserverPatch.cs` | `AIBattle.CheckGlobalAIStrategy` (6314), `AdjustGroupAIStance` (4221), `MicroAICheckForCharges` (4905), `CheckForFeudGroupActions` (4931), `CheckUseOfReserves` (6062), `LinkReservesToLineGroup` (6642), `AssignReserves` (7017), `CheckAIBombardment` (3869), `CheckLineFallbacks` (5118), `MicroAICheckForRetreats` (4817), `AIBattle.CheckCurrentOrderUpdate` (8233), `AIBattle.UpdateMovingTargets` (6870), `BattleUnits.SetWaypoint(Regiment, ...)` (91232), `Regiment.AddToOrderQueue` (124917), `Regiment.AddOrderCourierline` (125009) | Slice B0/B2/B3/default-off bug telemetry observer. Emits bounded read-only `[Tactical*]`, `[TacticalPlayerOrder]`, `[TacticalCommand]`, `[TacticalOdds]`, and order-friction telemetry for macro stance, group stance, charge/feud deltas, sectors/orders, odds/contact posture, reserves, artillery, fallback/retreat, W&L order delivery, command-tier scope, and delivered-vs-intended order state. When `Enable Tactical Bug Telemetry` is true, also emits focused `[TacticalCurrentOrder]`, `[TacticalWaypointDrift]`, `[TacticalCourierQueue]`, `[TacticalObjectiveMove]`, `[TacticalObjectiveMutation]`, `[TacticalReserveMove]`, and `[TacticalPathShape]` lines. When `Enable Tactical Decision Matrix Logging` is true and the tactical observer is enabled, emits high-volume `[TacticalDecisionMatrix]` battle/group rows: vanilla macro, group stance, Whiskey stance recommendation, W&L guard result, settlement-gate result, sector odds/mission, enemy/contact/flank/cover/path/order state. Matrix rows are removable by config and capped by `Tactical Decision Matrix Max Rows`. Does not change `macroai`, `ai_stance`, movement orders, reserve lists, artillery behavior, fallback/retreat state, order queues, path status, or persistence. B0 runtime smoke closed on 2026-05-07: all required tactical families fired, `[TacticalPlayerOrder] relation=ai-to-player-subordinate ... targetUnderCommander=True` proved the control surface, and no tactical observer failures were found. Live matrix smoke emitted 37,250 matrix rows without exception spam and fixed a matrix-only W&L/order-friction label inversion; group rows now include `pathInterrupted` and parseable `fort=0/1`, B3 sector extraction no longer floors no-contact sectors to enemy strength 1, matrix order-friction truth now uses the same `TacticalOrderSettlementGate` as the B5 writer, and non-brigade group rows now report `whiskeyReason=command-scope` instead of a writable local stance recommendation. Current local DLL `b1c0d76487d6a238d4f3cc047a299980ad60edff5b26e1d618bb5b43a807322f` includes the B5 settlement, tiny-contact, and command-scope follow-ups. Focused smoke on the prior telemetry DLL emitted `[TacticalCommand]` 25 times, `[TacticalOrder]` 116 times, `[TacticalCourierQueue]` five times, `[TacticalObjectiveMove]` eleven times, and live `[TacticalPathShape]` evidence including `reason=backward-first-segment`; fresh runtime proof is pending after restart on the current DLL. |
| 36 | `DispatchStanceSanitizerPatch` | Postfix | `Patches/DispatchStanceSanitizerPatch.cs` | `Messages.Message.GenerateMessageContent` (195447) | W&L campaign dispatch text sanitizer. After vanilla renders message content, replaces only known stance-0 sentences such as "instructions that are to none" for player-chain W&L messages of types 15/56/57. Does not edit `GameVars.groupstancename[0]`, does not suppress messages, and does not change current-order state. Logs bounded `[W&LDispatch]` first-sanitized markers. |
| 37 | `EconomyFilterMapAssignFiltersBootstrapPatch` | Prefix | `Patches/EconomyFilterMapAssignFiltersBootstrapPatch.cs` | `BattlefieldSetup.AssignFilters` (25067) | Vanilla startup-order guard for W&L economy filter maps. Before `AssignFilters` dereferences `Economy.availableworkforce/slavery/tradeandsupply/supply/availablecapital/transportbottlenecks/marketcapacity/hospitals.maptexture`, initializes any missing map using vanilla `UpdateFilterMaps` parameters. This lets vanilla finish `AssignFilters`, including `productionmapsymbols` creation, instead of leaving later `Economy.UpdateFilterMaps` full-cycle tails to hit `productionmapsymbols.ResetValues()` null. Logs bounded `[Patch:FilterMapAssignFilters]` bootstrap evidence; #31 runtime diagnostics now include production-symbol health if the tail guard still fires. Build/deploy/hash verified in local DLL `08c72fc67a97c26a97ca227b0ae640d41be81784a3e01cd890c3b7a4bcc54bdd`; fresh-launch smoke confirmed on 2026-05-06 with one bootstrap line and no `BattlefieldSetup.AssignFilters` / `Economy.UpdateFilterMaps` stack recurrence. |
| 38 | `CoordinatedOffensiveOperationsPatch` + `CoordinatedOffensiveMicroMovementPatch` | Prefix/Postfix/Finalizer | `Patches/CoordinatedOffensiveOperationsPatch.cs`, `Patches/CoordinatedOffensiveMicroMovementPatch.cs` | `AICampaign.CheckOffensiveMovements` (14166), `AICampaign.UpdateMicroMovementInOffensive` (13968) | Coordinated offensive package steering. When Whiskey selects a coordinated package, commits it through `CoordinatedOperationRuntime`/`WlStrategicOrderBridge` against the resolved vanilla offensive target, then empties `aifaction[i].ownunits` only for that vanilla call so vanilla cannot broaden or silently retarget the selected package. Restores filtered lists in Postfix/Finalizer. If selected-package Prefix fails after snapshot registration, restores the original `ownunits` snapshot and removes the snapshot; if failure occurs before a snapshot exists, leaves vanilla candidate lists unchanged and logs the pre-snapshot boundary. Package-locked units with active paths are temporarily filtered from vanilla offensive micro-movement so continuation logic cannot retarget them before the initial package move is consumed. Logs `[CoordinatedOps]` package decisions, partial/no-commit failures, and `[CoordinatedOps:Perf]` slow filter samples. |
| 39 | `ProjectAppointmentObserverPatch` | Prefix/Postfix observer | `Patches/ProjectAppointmentObserverPatch.cs` | `Projects.AppointProject` (213384) | Project doctrine appointment telemetry. Prefix snapshots old project level and payment context; Postfix logs only real level increases, distinguishes subsidy appointments from W&L prestige/manual appointments, and never mutates appointment, subsidy, prestige, or project effects. Logs once key `project-appoint-observer` and bounded `[ProjectAppointed]` lines. Build/deploy/hash verified in local branch DLL `f504f99d7266e0cdbf70e502a4b9f8d010990f3dde290c766d683040f653373d`; `AppointProject` did not fire during short smoke, so `[ProjectAppointed]` remains conditional long-run telemetry. |
| 40 | `ProjectUnlockObserverPatch` | Prefix/Postfix observer | `Patches/ProjectUnlockObserverPatch.cs` | `Projects.CheckProjectUnlocks` (213569) | Project doctrine unlock telemetry. Prefix/Postfix snapshots before/after alliance project IDs and logs newly added project IDs from vanilla init seeding only; does not mutate unlock behavior, project lists, or project effects. Logs once key `project-unlock-observer` and bounded `[ProjectUnlock]` lines. Build/deploy/hash verified in local branch DLL `f504f99d7266e0cdbf70e502a4b9f8d010990f3dde290c766d683040f653373d`; fresh campaign smoke confirmed `[once:project-unlock-observer]`, with no `[ProjectUnlock]` additions in that scenario. |
| 41 | `BattleChargeGatePatch` | Prefix replacement | `Patches/BattleChargeGatePatch.cs` | `AIBattle.MicroAICheckForCharges` (4905) | Slice B1 default-off W&L tactical charge guard. Mirrors vanilla charge initiation/cancellation, runs after B0 observer Prefixes, blocks new player-subordinate charge initiation only when `Enable W&L Tactical Charge Guard` is true and W&L is active, preserves charge cancellation and `lastfeudactiontime`, and falls back to vanilla if required private anchors are missing before movement ownership. Build/deploy/hash verified in local DLL `534500bc498584059d198412765e67a9615e8b7376f4bc0aea6314b2404c1562`; fresh B1 runtime denial smoke was deferred by user direction on 2026-05-07. |
| 42 | `BattleFeudActionGatePatch` | Prefix replacement | `Patches/BattleFeudActionGatePatch.cs` | `AIBattle.CheckForFeudGroupActions` (4931) | Slice B1 default-off W&L feud movement guard. Mirrors vanilla feud eligibility/probability/timing and delayed `SetWaypoint(... useorderdelay: true ...)`, runs after B0 observer Prefixes, blocks protected player-subordinate group movement only when `Enable W&L Tactical Charge Guard` is true and W&L is active, and falls back to vanilla if required private anchors are missing before movement ownership. Build/deploy/hash verified in local DLL `534500bc498584059d198412765e67a9615e8b7376f4bc0aea6314b2404c1562`; fresh B1 runtime denial smoke was deferred by user direction on 2026-05-07. |
| 43 | `TacticalFallbackRetreatNullGuardPatch` | Finalizer | `Patches/TacticalFallbackRetreatNullGuardPatch.cs` | `AIBattle.MicroAICheckForRetreats` (4817), `AIBattle.CheckLineFallbacks` (5118) | Tactical battlefield bug guard. Default-off behind `Enable Tactical Fallback Retreat Null Guard` for focused smoke runs. Suppresses method-level `NullReferenceException` from those two vanilla fallback/retreat loops and logs one bounded warning; all non-null exceptions propagate. |
| 44 | `BattleMacroStrategyPatch` | Postfix | `Patches/BattleMacroStrategyPatch.cs` | `AIBattle.CheckGlobalAIStrategy` (6314) | Slice B4 default-off tactical macro stance scorer. Uses B3 odds/contact doctrine after vanilla macro logic, skips debug/save-restore/retreat/end-battle macro ownership, and only writes `macroai` through the narrow reflected field when `Enable Tactical Macro Stance Scorer` is true. Current runtime extraction reads `AIBattle.unitsused` for visible/inferred enemy strength, received-fire evidence, and sector assessments instead of treating global force balance as confirmed contact; no-contact odds stay zero. Logs bounded `[TacticalMacroDecision]` lines. The default-off gate is intentional because `macroai` is behavior-changing vanilla battle state; do not make it default-on before focused smoke proves stable anchors, bounded logs, no repeated exceptions, and no macro flip-flop. Build/deploy/hash verified in current local DLL `b1c0d76487d6a238d4f3cc047a299980ad60edff5b26e1d618bb5b43a807322f`; runtime behavior smoke for current B4 remains pending. |
| 45 | `BattleGroupStancePatch` | Postfix | `Patches/BattleGroupStancePatch.cs` | `AIBattle.AdjustGroupAIStance` (4221) | Slice B5 default-off group sector stance scorer. Uses B3 sector doctrine after vanilla group stance logic, preserves W&L player-subordinate chain guards, skips unsettled vanilla order cases through `TacticalOrderSettlementGate`, never issues movement/reserve/artillery/fallback/charge orders, and never writes charge stance 4. The settlement gate blocks queued orders, nonzero `orderstate`, unknown reflected order state, interrupted paths, and active movement mode before B5 writes `ai_stance`, matching vanilla's pending `BattleUnits.ChangeStance` / `Regiment.ProcessOrders` handoff instead of stacking orders. The local B5 writer is scoped to battle brigade groups only (`unittyp == 14`); division/army group command remains vanilla/B6-owned, and #35 matrix rows report `whiskeyReason=command-scope` for non-local group tiers. Logs bounded `[TacticalGroupDecision]` lines. The sector ledger treats no measured enemy strength as zero attack odds and requires angle-slice contacts to meet a minimum enemy-strength/share gate before becoming decisive, so phantom or tiny-detachment `AttackWeakPoint` sectors cannot drive stance changes. Under defensive macro (`macroai == 2`), defensive `AttackWeakPoint`, `Fix`, and `EconomyOfForce` sectors apply stance 2 `defend-hold`, matching vanilla's defensive stance ownership instead of issuing stance-1 probes from local doctrine. Explicit Probe sectors can still bypass the general low-confidence skip at `confidence >= 0.35`, and attack/assault macro weak points still apply stance 3 `attack-weak-point`. `macroai == 3` is skipped as `vanilla-retreat`, matching vanilla `CheckGlobalAIStrategy()` retreat ownership and `AdjustGroupAIStance()` stance-1 retreat mapping. The default-off gate is intentional because `ai_stance` is behavior-changing vanilla battle state; do not make it default-on before focused smoke proves no player-subordinate retasking, no charge stance 4 writes, no repeated exceptions, and no movement/reserve/artillery/fallback side effects. Build/deploy/hash verified in current local DLL `b1c0d76487d6a238d4f3cc047a299980ad60edff5b26e1d618bb5b43a807322f`; focused runtime behavior smoke for this B5 follow-up remains pending after restart. |
| 46 | `BattleObjectiveChainWlGuardPatch` | Prefix/Postfix filter/restore | `Patches/BattleObjectiveChainWlGuardPatch.cs` | `AIBattle.UpdateMovingTargets` (6870) | Default-off W&L objective-chain guard under `Enable W&L Tactical Charge Guard`. Vanilla checks only `linegroup_centerunit.dlcw_isundercommander`; this patch filters objective-chain entries whose center group is player-commanded or carries attached player-subordinate units for the duration of vanilla `UpdateMovingTargets`, then restores the list in Postfix. It blocks objective-chain advance without deleting chains or issuing movement, and logs bounded `[TacticalObjectiveGuard] denied objective-chain advance ... reason=player-subordinate-attached`. Build/deploy/hash verified in current DLL `b1c0d76487d6a238d4f3cc047a299980ad60edff5b26e1d618bb5b43a807322f`; runtime denial marker still needs smoke. |
| 47 | `BattleCommanderIntentObserverPatch` | Postfix | `Patches/BattleCommanderIntentObserverPatch.cs` | `AIBattle.AdjustGroupAIStance` (4221) | Slice B6a default-off telemetry observer. Runs after vanilla group stance, derives commander intent (`TacticalCommanderIntentResolver`) and playbook (`TacticalPlaybookLedger`) from vanilla `macroai`, `objectivechain[i].linegroup_centerunit`, and per-side reserve/anchored-flank evidence, then emits bounded `[TacticalIntent]` and `[TacticalPlaybook]` lines under `Enable Tactical Commander Intent Doctrine`. Does not change `macroai`, `ai_stance`, movement orders, reserve lists, artillery behavior, fallback/retreat state, order queues, path status, or persistence. Build/deploy/hash verified in DLL `eec159cae76675b0646a8ea7992bc2711a645025b488b72bf52ac140e88e4ffc` (602112 bytes); runtime telemetry smoke is pending. |
| 48 | `B7CheckAIBombardmentPatch` | Postfix | `Patches/B7CheckAIBombardmentPatch.cs` | `AIBattle.CheckAIBombardment` (3869), private `AIBattle.PerformAIActionDLCWL` (5101) | Slice B7 default-off artillery doctrine writer. After vanilla bombardment logic, builds `TacticalArtilleryDoctrine` inputs from attached artillery and can use vanilla `BattleUnits.ChangeCombatBehavior` to cancel bombardment, convert active bombardment to counter-battery, or preserve fire only when `Enable Tactical Artillery Doctrine` is true. Preserves vanilla artillery gates, W&L/player-control guard, and `PerformAIActionDLCWL`; does not duplicate `CheckArtyFallback` movement. Logs `[once:b7-check-ai-bombardment]` and bounded writer markers such as `[once:b7-cancel-bombard]` / `[once:b7-counterbattery]`. Build/deploy/hash verified in DLL `328c74a43f356df4ecb52f38a0df1ec89267eae714fd207af406529b1adffef0` (633856 bytes). Focused smoke with live config `Enable Tactical Artillery Doctrine = true` confirmed `[once:b7-check-ai-bombardment]` and `[once:b7-counterbattery]` with no B7 exception/Harmony/missing-anchor/error markers; conditional `[once:b7-cancel-bombard]` has not fired yet. |
| 49 | `B8CheckLineFallbacksObserverPatch` | Postfix observer | `Patches/B8CheckLineFallbacksObserverPatch.cs` | `AIBattle.CheckLineFallbacks` (5118) | Slice B8 telemetry-only observer. Emits `[once:b8-check-line-fallbacks]` when the vanilla line-fallback loop fires. Does not write fallback state, movement orders, `ai_stance`, withdrawal state, or tactical ledger state. Build/deploy/hash verified in DLL `328c74a43f356df4ecb52f38a0df1ec89267eae714fd207af406529b1adffef0` (633856 bytes); first-fire runtime smoke confirmed with no B8 exception/Harmony/missing-anchor/error markers. |
| 50 | `B8MicroAICheckForRetreatsObserverPatch` | Postfix observer | `Patches/B8MicroAICheckForRetreatsObserverPatch.cs` | `AIBattle.MicroAICheckForRetreats` (4817) | Slice B8 telemetry-only observer. Emits `[once:b8-microai-check-retreats]` when the vanilla retreat micro-AI loop fires. Does not write retreat state, movement orders, withdrawal state, `ai_stance`, or tactical ledger state. Build/deploy/hash verified in DLL `328c74a43f356df4ecb52f38a0df1ec89267eae714fd207af406529b1adffef0` (633856 bytes); runtime smoke is pending. |
| 51 | `B8MoraleSnapshotSamplerPatch` | Postfix observer/sampler | `Patches/B8MoraleSnapshotSamplerPatch.cs` | `AIBattle.MicroAICheckForCharges(Regiment, int)` (4905) | Slice B8 morale snapshot sampler. Runs on vanilla's existing micro-AI charge cadence, samples eligible attached units into `Plugin.MoraleSnapshotLedger` / `TacticalMoraleSnapshotLedger`, prunes routed units, and emits `[once:b8-morale-snapshot-sampler]`. Does not write vanilla battle state, charge state, movement orders, fallback/retreat state, or persistence. Build/deploy/hash verified in DLL `328c74a43f356df4ecb52f38a0df1ec89267eae714fd207af406529b1adffef0` (633856 bytes); first-fire runtime smoke confirmed with no B8 exception/Harmony/missing-anchor/error markers. |
| 52 | `B8CheckUseOfReservesPatch` | Postfix telemetry + default-off writer | `Patches/B8CheckUseOfReservesPatch.cs` | `AIBattle.CheckUseOfReserves` (6062), `BattleUnits.SetWithdrawal` (92821), private `AIBattle.PerformAIActionDLCWL` (5101) | Slice B8 withdrawal/help-request wiring. After vanilla reserve-use logic, scores attached infantry/cavalry units with `TacticalWithdrawalDoctrine`, emits one aggregated `TacticalSectorLedger.SetHelpRequest` per group, and only when `Enable Tactical Withdrawal Doctrine` is true can call static `BattleUnits.SetWithdrawal` for conservative withdrawal candidates. Uses campaign `Tools.Date(...).yearfraction` for `SetWithdrawal`, preserves W&L/player-control gates through `PerformAIActionDLCWL`, and never mutates reserve lists or calls `RegimentSetPath`, `SetWaypoint`, `SetMovementMode`, or instance `Regiment.SetWithdrawal`. Logs `[once:b8-check-reserves]` and default-off writer marker `[once:b8-set-withdrawal]`. Build/deploy/hash verified in DLL `328c74a43f356df4ecb52f38a0df1ec89267eae714fd207af406529b1adffef0` (633856 bytes). Focused smoke with live config `Enable Tactical Withdrawal Doctrine = true` confirmed `[once:b8-check-reserves]` and `[once:b8-set-withdrawal] B8 SetWithdrawal applied count=1` with no B8 exception/Harmony/missing-anchor/error markers. |
| 53 | `TacticalPathfinderDisciplinePatch` | Prefix/Postfix | `Patches/TacticalPathfinderDisciplinePatch.cs` | private `Regiment.AddPath` (130259), called by `Regiment.RegimentSetPath` retry loop (131167) | BUG-TAC-010 default-off tactical pathfinder discipline under `Enable Tactical Pathfinder Discipline`. Captures the path count before vanilla `AddPath`, then after vanilla removes failed non-target fragments that would poison retry starts, rejects non-complete NavMesh paths accepted after retry exhaustion, and treats complete path endpoints within 5m as success instead of requiring exact X/Z equality. Skips campaign units and fleets. Logs bounded `[TacticalPathfinderDiscipline] reason=endpoint-within-tolerance|navmesh-noncomplete|failed-endpoint-mismatch`. Console harness 511 PASS / 0 FAIL; build/deploy/hash verified in DLL `b1c0d76487d6a238d4f3cc047a299980ad60edff5b26e1d618bb5b43a807322f` (639488 bytes). Runtime enabled smoke is pending. |
| — | `WlStrategicOrderBridge` | Coordinator/helper/runtime | `Strategic/WlStrategicOrderBridge.cs` | called by operational probe, army-area, coastal-defense, and coordinated-operation runtime conversions | Central W&L strategic order bridge. Classifies player role state without Unity dependencies for tests, then live adapters call vanilla `AIBattle.CheckCurrentOrderUpdate(..., calledfromcampaign:true)` only when the W&L chain gate should pass. Null requests fail closed, player-CIC/player-controlled/player-unit calls block direct movement, and failed or ineligible player-chain bridge calls log and skip instead of direct-moving or mutating operation lists. |
| — | `CoastalDefenseCustomOrderRunner` | Coordinator/helper/runtime | `Strategic/CoastalDefenseCustomOrderRunner.cs` | weekly `StrategicCoordinator.UpdateDefenseIntent`; per-tick release tracking against `unitsindefensiveoperations` | Coordinator-driven defensive movement order runner for true `SeaInvasion` / `RaidForce` active responses only. It delegates to `DefenseCustomOrderPolicy`, so `AssetProximity` never issues custom `MoveUnitTo` orders. Ledger-selected candidates must first pass `StrategicMovementBudget` formation/front/capital caps; W&L current-order attempts are tracked without adding units to defensive operation lists, and direct movement records assignment only if `MoveUnitTo` succeeds. Capital defense remains owned exclusively by #4. Defense intent slice 2 enforcement surface 2. |
| — | `OperationalProbeRuntime` | Coordinator/helper/runtime | `Strategic/OperationalProbeRuntime.cs` + `Strategic/OperationalProbeLedger.cs` + `Strategic/OperationalTempoDoctrine.cs` | daily `StrategicCoordinator.UpdateOperationalProbe`; uses vanilla `Policy.CurrentChapter`, `AICampaign.MoveUnitTo`, and `unitsinoffensiveoperations` | Strategic operational probe/contact loop. Starts at most one eligible same-area probe, refuses critical hold donors, pauses on enemy reaction, withdraws when overmatched, escalates after favorable contact, overlays `FormationDirectiveLedger`, and commits/removes the selected formation through vanilla offensive-operation surfaces. Probe duration, size, and escalation thresholds are paced by vanilla chapter, Whiskey era, season, faction, and CIC personality. Operation phase metadata can now adjust probe/package thresholds through `OperationPosture`, target package X/Z through objective target coordinates, and package behavior through `AllowCoordinatedAttack`, `AllowReinforcementPackage`, and `AllowProbeOnly`. Living reference: `docs/operational-tempo-doctrine.md`. Does not touch tactical battle AI. |
| — | `HistoricalOperationCatalog` / `OperationDynamicRuleEvaluator` | Coordinator/helper/runtime | `Strategic/HistoricalOperationCatalog.cs`, `Strategic/HistoricalOperationContextBuilder.cs`, `Strategic/OperationDynamicRuleEvaluator.cs`, `Strategic/OperationDecisionMemory.cs` | `CIC.Replan`, `StrategicCoordinator.BuildPhaseTruth`, `PhaseTruthLedger.Evaluate`; no direct Harmony target | Named historical operation doctrine for CIC plans. Resolves explicit operation profiles from scored vanilla objectives; default-on config `Enable Historical Operation Doctrine`; catalog misses log `NoProfile` and do not create generic replacement plans while enabled. `PickCampaignObjectivePatch` blocks vanilla random objective fallback when doctrine is enabled and the CIC has no historical plan. Dynamic rules can advance, complete, recover, pause, abort, exploit, counterstroke, screen/delay, or pivot to an explicit alternate through phase truth. Operation identity, posture, tempo, phase metadata, and recent replan memory persist in the sidecar. |

**Note on UI/startup patches:** #11, #12, #14, and #22 touch menu/startup UI methods, which are volatile across game patches. If a future GTCW update breaks them, the OnceLog markers stay silent or a one-time boundary warning appears. Workaround for affected settings-lock users: set `OverrideVanillaSettings = false` in `BepInEx/config/dev.kyle.whiskey-realism.cfg` and pick the recommended values manually (Mediocre aggression, Historic personality, Hard difficulty, realism checkboxes on) until a mod update lands.

**Persistence patches (not numbered — symmetric pair):**

| Patch | Type | File | Targets (decompile line) | Description |
|---|---|---|---|---|
| `AICampaignSaveLoadPatch.SavePatch` | Postfix | `Patches/AICampaignSaveLoadPatch.cs` | `AICampaign.Save` (16631) | Writes `whiskeyrealism.json` sidecar inside the save folder. |
| `AICampaignSaveLoadPatch.LoadPatch` | Postfix | `Patches/AICampaignSaveLoadPatch.cs` | `AICampaign.Load` (16435) | Reads sidecar; falls back to fresh init with explicit log if missing. |

---

## Conventions

- **Ordinal stability:** Once assigned, a number is never re-used. Withdrawn patches keep their ordinal with a `(withdrawn)` note. This makes git-log and changelog references stable across time.
- **One concern per file:** Every patch class lives in its own `.cs` file under `src/WhiskeyRealism/Patches/`; helper/runtime catalog rows may live under `src/WhiskeyRealism/Strategic/`.
- **Targets column** lists `Class.Method` and the decompile line number from `/tmp/gt_src/asm/Assembly-CSharp.decompiled.cs`.
- **Source-of-truth order:** shipped code > this catalog > per-patch design doc > umbrella spec > archived plan. If they disagree, trust the code.

## v0.2.1 — what shipped

- **#2 `ImportanceValuesPatch` redesigned** — moved from `AICampaign.UpdateImportanceValues` (wrong shape) to `AIArea.CalculateMostValueableAIZones` (Postfix override of `mostvalueableaiareaclose`). Catalog ordinal preserved.
- **#6 `CommanderReplacementPatch` upgraded** — gate-only stub replaced with concrete `AssignCommando` + `DoCommanderPromotion` swap when scripted succession events fire. `AppliedEventIds` tracking persisted in sidecar.
- **`Strategic/WarStateObserver.cs`** — town-ownership observers for Vicksburg / Chattanooga / Atlanta. Unlocks succession events #8 (Grant→GiC), #9 (Sherman→Western), #10 (Hood replaces Johnston) which previously could never fire.

## Pending

Next unreserved patch ordinal is #54. #47 `BattleCommanderIntentObserverPatch` is the B6a default-off telemetry observer (telemetry-only; no vanilla writes); runtime `[TacticalIntent]`/`[TacticalPlaybook]` smoke is pending. #48-#52 are B7+B8 tactical runtime wiring; B7 and B8 writer surfaces remain default-off in C# defaults, but the local smoke config currently has `Enable Tactical Artillery Doctrine = true` and `Enable Tactical Withdrawal Doctrine = true`. Runtime smoke confirmed #48 first-fire/counterbattery, #49 first-fire, #51 first-fire, and #52 reserve/withdrawal writer; #50 `[once:b8-microai-check-retreats]` and #48 conditional `[once:b7-cancel-bombard]` remain pending. #46 objective-chain guard is implemented and hash-deployed behind the existing W&L tactical guard switch. #53 pathfinder discipline is implemented and hash-deployed behind `Enable Tactical Pathfinder Discipline`; runtime enabled smoke is pending. B4/B5 remain default-off in C# defaults because they write vanilla battle state; the local smoke config has them enabled. Fresh runtime behavior smoke on DLL `b1c0d76487d6a238d4f3cc047a299980ad60edff5b26e1d618bb5b43a807322f` should verify B5 queued/pending-order skip behavior, tiny-contact weak-point suppression, `defend-hold` / `command-scope` rows, and #53 pathfinder markers before any default-on discussion.

**v0.2.2 also includes:** using the #5 battle-history observer inside `WarStateObserver` to unlock the remaining 7 succession events (#1, #3, #4, #5, #6, #11, #12 — gated on ANV defeats / AoP failures / Burnside's first defeat / Lee invading Pennsylvania / Western theater defeats / Valley operations / war clearly lost). `FrontSectorLedger` is now the transfer-budget guard for #3. `ArmyAreaLedger` + #15/#16 steer historical operating areas and army-group organization. `FormationDirectiveLedger` classifies independent divisions/corps/armies, supplies non-spammy `[FormationDirective]` telemetry, and gates #15/#16 division behavior. `GrandStrategyProfile` + objective tags + #17 steer objective/project choice; #19 extends that profile into policy timing. #22 protects the W&L career-start command-selection popup after the lag fix removed startup overhead. Post-release main also routes locked-Hard difficulty into `CIC.Effective` as a small historical casualty-tolerance modifier, adds #7 role-aware campaign army/fleet perk steering, includes construction steering Slice B via #20/#24, adds `CampaignMapLedger` town/state/fort/harbor awareness for recruitment, construction, theater classification, and proportional capital-defense sizing, implements defense intent ledger Slice 1+2 via `DefenseIntentLedger` + `DefenseIntentRuntime` + `AssetStrategicRole`/`AssetRoleCatalog` + optimized daily operational cadence + #25 candidate-filter Prefix/Postfix + `CoastalDefenseCustomOrderRunner` + #4 `[HarmonyPriority(Priority.High)]` ordering + capital invariant lift, and adds #26 high-speed vanilla AI governor after a stutter investigation found vanilla 20x frames running 4 `UpdateUnitAI` passes in 26ms. Follow-up log tails found remaining once-per-day spikes in `defenseMs`; `DefenseIntentRuntime.GetXZDistance()` now avoids reflection in asset/enemy/candidate inner loops, the defense runtime uses typed `Regiment` reads and one `Frontline2` lookup per pass, and #4 caches the corrected nested `FortConstructionOrder.UnitAlreadyConstructing` method to remove repeated Harmony warnings after restart. Dynamic vanilla-created command names now receive local active-map army-area fallback doctrine while known off-map full-war army names remain unassigned. Recruitment state steering now protects threatened capital-priority areas before broader theater steering. #27 now filters saturated vanilla fort-construction sites so neither side can stack unlimited local/capital forts without local enemy pressure, and hard-caps clusters even under threat. The 2026-05-05 strategic anti-zerg/theater-integrity fix makes `AssetProximity` local-only, blocks asset custom-orders, routes defense candidates through formation/front budgets, caps capital-defense packages, extends #25 filtering, and gates #15 return-area movement through front budget. The 2026-05-05 operational probe/contact loop adds one-formation probes, enemy-reaction pauses, overmatch withdrawal, favorable-contact escalation, formation-directive overlays, and chapter/era/season/faction/personality tempo before mass commitment. Slice B tactical observer #35 now includes B0/B2/B3 telemetry, while #44/#45 provide default-off B4/B5 behavior valves pending runtime smoke. Remaining Slice A follow-up is opportunistic (guard-budget/cooldown tuning from runtime telemetry, asset-name catalog refinement from non-East scenarios, probe threshold tuning from `[OperationalProbe]` logs).
