# Vanilla Tick-System Bugs

This queue holds narrow bugs found during the 2026-05-05 vanilla campaign tick review. It is not a broad AI cadence spec.

## Findings

| ID | Status | Failure mode | Vanilla / patch anchor | Evidence | Narrow fix direction |
|---|---|---|---|---|---|
| `BUG-TICK-001` | In progress | Paused campaigns can still run one `UpdateUnitAI()` pass per `AICampaign.Update` frame after vanilla AI initializes. | Vanilla `AICampaign.Update` starts at decompile line 11159 and computes `Mathf.Max(1, FloorToInt(Pow(GameVars.gamespeed, 0.5f)))` at 11195. `GameVars.SetGameSpeed(0f)` sets `gamepaused=true` and `uniStormSystem.timeStopped=true` at 66368-66379, but `AICampaign.Update` does not check either flag. Whiskey #26 `CampaignAiUpdateGovernorPatch` owns the patch surface. | Code-level confirmed against vanilla decompile. Regression coverage added for paused/game-speed-zero skip behavior. Clean staged-tree tests/build passed; deployed DLL SHA-256 is `4e01274babb3af3def1143ab4acee6db8a31c27817d80c1789385b60e3c0f19f`. | Keep vanilla `InitializeAI()` behavior intact, then skip already-initialized `AICampaign.Update` when `GameVars.gamepaused` is true or `GameVars.gamespeed <= 0f`. Fresh-launch smoke still pending before marking shipped. |
| `BUG-TICK-002` | Shipped | Campaign economy initialization can spin indefinitely if `Economy.UpdateFilterMaps(initialization:true)` keeps returning false after a stuck iterator or repeated exception path; runtime `UpdateFilterMaps(false)` can also rethrow the same `NullReferenceException` every campaign frame if vanilla startup leaves filter-map symbols uninitialized. | `BattleUnits.CampaignDataRuns` calls `while (!Economy.UpdateFilterMaps(initialization: true)) { }` at decompile line 79874. Runtime `BattleUnits.Update` calls `Economy.UpdateFilterMaps()` at 79341. `Economy.UpdateFilterMaps` starts at 31852; #31 `EconomyFilterMapInitializationGuardPatch` owns the guard. `BattlefieldSetup.AssignFilters` starts at 25067; #37 `EconomyFilterMapAssignFiltersBootstrapPatch` owns the root startup-order fix. | Startup smoke confirmed on merged DLL `5a8bf2c7…` (2026-05-05): two bounded `[once:filter-map-init:exception:NullReferenceException@827/49:827/615:213/214]` lines fired during startup with distinct iterator signatures, then startup advanced. Fresh 2026-05-06 Player.log then showed `BattlefieldSetup.AssignFilters` throwing before #31 runtime diagnostics reported `UpdateFilterMaps` failures after vanilla had already advanced/reset the cursor, with filter maps/globals/current buildings valid. That pointed to the earlier `AssignFilters` abort leaving `BattlefieldSetup.productionmapsymbols` unavailable for line 32113. Post-#37 smoke on DLL `08c72fc6…` logged exactly one `[Patch:FilterMapAssignFilters]` bootstrap line and no `BattlefieldSetup.AssignFilters` / `Economy.UpdateFilterMaps` stack recurrence through 1861-06-17. | #31 still bounds the initialization/runtime iterator failure. #37 initializes any missing vanilla economy `FilterMap` textures before `BattlefieldSetup.AssignFilters` dereferences them, allowing vanilla to finish `productionmapsymbols` setup. Runtime diagnostics include production-symbol health if #31 still fires. |
| `BUG-TICK-003` | Shipped / smoke refresh pending | W&L diary event updates can throw `NullReferenceException` under `BattleUnits.Update` when the diary queue starts or resumes before W&L player-command, diary dependencies, or cycle-specific campaign data are fully initialized. | `BattleUnits.Update` calls `Diary.UpdateEventQueue()` at decompile line 79356. `Diary.UpdateEventQueue` only checks `GameVars.frame >= 50` at 182889 before calling `UpdateEvents`, then increments `updatecycle` only after `UpdateEvents` returns. `Diary.UpdateEvents` starts at 182902 and dereferences `GameVars.commander[DLC_WL.dlc_chosencommander].currentcommand` through `BattleUnits.GetCampaignGroup(...)` at 182925, `weather.GetClimateData(...)` at 182965, `diaryevents[DiaryEvent.weightincrease]` at 182986/182997, `DLC_WL.food.Count` at 183025, `cardinalpoints` via `GetDirection` at 182948/183185, plus cycle-specific nation, parent-commander, closest-town, group-supply, friendly-operation, and camp-station data. Whiskey #29 owns the existing `Diary.UpdateEvents` patch surface. | Post-#37 fresh smoke showed exactly two early `Diary.DMD<Diary::UpdateEvents>() -> Diary.UpdateEventQueue() -> BattleUnits.Update()` NREs while logs still had W&L startup command-readiness churn. Fresh 2026-05-11 Player.log then showed repeated recurrences after one expected `[once:wl-diary-startup:skip]` line, proving the gate could still fall through after core startup dependencies appeared while the active vanilla update cycle still had unsafe campaign data. Regression coverage now includes imported dependencies, unsafe campaign-group lookup, and unsafe active-cycle preflight. Console harness is **842 PASS / 0 FAIL**. `./build.sh` passes with 0 warnings / 0 errors. Local DLL built/deployed/hash-verified as `8151bd2417a5d7b3363aca30fe7eb88338c05d12702a17443de626217d4eb79a` (937472 bytes). | #29 folds the readiness gate into the existing `Diary.UpdateEvents` Prefix before incrementing W&L camp threshold scope depth. It skips only W&L diary updates until selected commander/current command, safe `BattleUnits.GetCampaignGroup(currentcommand)` lookup, imported diary events, cardinal points, food data, weather when needed, and the active vanilla update cycle are ready. Readiness-check exceptions now fail closed for that diary tick; W&L `NullReferenceException` fallthrough from `Diary.UpdateEvents` is contained with a bounded warning so `Diary.UpdateEventQueue` can advance rather than pinning a failing cycle. Fresh-launch smoke refresh is pending. |

## Not Verified

- Long-run smoke for `BUG-TICK-001` on merged DLL `5a8bf2c7…`; verify the paused first-fire marker appears at most once when the player pauses mid-campaign.

## Immediate Follow-Up

1. Pause-tick smoke for `BUG-TICK-001`: pause a running campaign and confirm a single `[once:campaign-ai-paused]` line, no repeated pause spam.
